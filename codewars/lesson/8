//Напишите функцию, которая удаляет пробелы из строки, а затем возвращает результирующую строку.
<?php
function no_space($str)
{
    return preg_replace("/\s+/", "", $str);
}

$str = "пряник лучший кот";
echo no_space($str);
